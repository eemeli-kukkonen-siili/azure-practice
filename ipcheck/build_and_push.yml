# Run with command (from parent directory): az acr run --registry <your_registry_name> --file build_and_push.yml .ipcheck
version: v1.1.0
steps:
  - build: -t $Registry/ipcheck:$ID .
  - push: ["$Registry/ipcheck:$ID"]